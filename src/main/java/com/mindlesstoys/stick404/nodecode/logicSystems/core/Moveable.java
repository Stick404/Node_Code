package com.mindlesstoys.stick404.nodecode.logicSystems.core;

/** This is to check if a Node is moveable, and if not, why **/
public enum Moveable {
    MOVEABLE,
    LOCKED_PLAYER,
    LOCKED_BLOCK
}
